kind = "api"
previewPath = "/api"
title = "API Server"
version = "1.0.0"
id = "3B4_FFSkEVBkAeYMFRJ2e"

[[services]]
localPort = 8080
name = "API Server"
paths = ["/api"]

[services.development]
run = "bash -c 'cd /home/runner/workspace/instagram-auto-liker/backend && uvicorn app.main:app --host 0.0.0.0 --port 8080 --reload'"

[services.env]
PORT = "8080"
